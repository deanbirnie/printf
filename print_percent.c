#include "main.h"

/**
 * print_percent - prints the % sign to console
 *
 * Return: returns 1, the number of characters printed to the console
 */
int print_percent(void)
{
	_putchar('%');

	return (1);
}
