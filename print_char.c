#include "main.h"

/**
 * print_char - prints characters as a helper for printf fucntion
 *
 * @c: character to print to console
 *
 * Return: returns 1 for the number of characters printed
 */
int print_char(char c)
{
	_putchar(c);

	return (1);
}
